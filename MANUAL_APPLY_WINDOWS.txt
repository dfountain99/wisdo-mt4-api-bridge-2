WISDO MAJOR UPGRADE - WINDOWS APPLY / VERIFY
2026-08-09

Repository:
C:\Users\jaque\Documents\wisdo-mt4-api-bridge-2

Required branch:
codex/wisdo-major-stability-upgrade-2026-08-09

1) CONFIRM BRANCH
   git branch --show-current
   git status --short

2) KEEP THE ORIGINAL SAFETY PATCH OUTSIDE THE REPO
   If WISDO_CODEX_WIP.patch is still in the repo, move it to Desktop before committing:
   move WISDO_CODEX_WIP.patch "%USERPROFILE%\Desktop\WISDO_CODEX_WIP.patch"

3) OVERLAY THE CONTENTS OF WISDO_MAJOR_UPGRADE_MANUAL_FINISH.zip INTO THIS REPOSITORY.
   The finished ZIP intentionally excludes .git, node_modules, .env files with secrets, and temporary audit files.

4) CLEAN DEPENDENCIES FROM THE LOCKFILE
   npm ci

5) RUN GATES
   npm run check
   npm run lint
   npm run test:unit
   npm run test:integration
   npm run test:commands
   npm run smoke
   npm run audit:runtime
   npm run audit:commands
   npm run audit:stubs
   npm run pressure:mt4
   npm audit --omit=dev

6) POSTGRESQL STAGING GATE - DO NOT POINT THIS AT PRODUCTION FIRST
   Set DATABASE_URL to a disposable/staging PostgreSQL database.
   npm run migrate:postgres
   npm run migrate:postgres

   Both runs must succeed. Inspect the resulting tables/indexes/foreign keys before production use.

7) DO NOT ENABLE LIVE VOICE TRADING
   WISDO_VOICE_EXECUTION_MODE=DEMO_ONLY

8) REVIEW SOURCE DIFF
   git status --short
   git diff --check
   git diff --stat

9) DO NOT PUSH OR DEPLOY UNTIL THE STAGING DATABASE + AUTH + DEMO REPORTER + SQUARE SANDBOX CHECKS PASS.
